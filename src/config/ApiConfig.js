const url = "http://13.126.152.107/";

const user = `${url}api/`;
const ApiConfig = {
    loginAdmin: `${user}login_admin`,
    getAllBlogs: `${user}get_all_blogs`,
    
};

export default ApiConfig;
